# Backend API base URL (no trailing slash). Leave empty in dev to use Vite proxy.
VITE_API_URL=

# Production: https://ttp-dashboard-api.azurewebsites.net
# If REQUIRE_API_KEY=true on backend, set the same key here (e.g. ttp-dashboard-secret-key-2024-secure)
VITE_API_KEY=
